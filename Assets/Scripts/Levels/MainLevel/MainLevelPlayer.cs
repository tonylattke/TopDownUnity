using UnityEngine;

public class MainLevelPlayer : BasePlayer
{
    private new void Start()
    {
        base.Start();
    }

    // Update is called once per frame
    private new void Update()
    {
        base.Update();
    }
}
